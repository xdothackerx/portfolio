<div class="row" align="center">
  <h1>Stuff I've Done</h1><br />
  <p>A gallery of the coding projects I've done so far, big and small!</p>

  <div id="projects" class="large-block-grid-4 text-center">
    <%= render @projects %>
  </div>
  <% if policy(@projects).publish? %>
    <%= link_to 'New Project', new_project_path, id: :new_link, remote: true %>
  <% end %>
</div>
